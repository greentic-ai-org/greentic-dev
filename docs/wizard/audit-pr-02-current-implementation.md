# Wizard Audit (PR-02): Historical Baseline

Date: 2026-03-02
Status: historical reference only (superseded by PR-03 launcher-only implementation)

This document captures the pre-PR-03 baseline and is intentionally retained as historical context.

## Important

The implementation in `master` no longer matches this baseline.

Major changes since this audit:

- `wizard run` removed
- `wizard replay` removed
- launcher-only identity introduced (`launcher.main`)
- strict AnswerDocument identity enforced
- legacy per-target mappings removed from `greentic-dev` wizard

For current behavior, use:

- `docs/wizard/README.md`
- `.codex/PR-03-stage2_surgical_update.md`

